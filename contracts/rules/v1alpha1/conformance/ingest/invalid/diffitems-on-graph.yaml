apiVersion: rules.flowconsole.io/v1alpha1
kind: RuleFile
rules:
  - id: bad-graph
    name: Graph cannot use diffItems
    kind: graph
    target: observed
    severity: error
    blocking: true
    subject:
      entity: diffItems
    assert: "true"
    message: This is invalid by design
